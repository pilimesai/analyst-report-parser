name: Update Active ETF Holdings Daily

on:
  schedule:
    # 每天台灣時間 20:30 (UTC 12:30) 自動執行
    - cron: '30 12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch Active ETF Holdings
        run: |
          python update_active_etf.py

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
          git add active_etf_holdings.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: auto-update active ETF holdings from Goal Star" && git push origin main)
